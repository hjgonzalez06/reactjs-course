// constants

let initialData = {
    loggedIn: false
};
//let login = "LOGIN";

// reducer

export default function reducer(state = initialData,action){

    switch(action.type){
        default:
            return state;
    }

}

// actions (action creator)